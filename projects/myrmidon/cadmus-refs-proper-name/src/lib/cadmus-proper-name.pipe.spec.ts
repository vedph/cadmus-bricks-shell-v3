import { CadmusProperNamePipe } from './cadmus-proper-name.pipe';

describe('CadmusProperNamePipe', () => {
  it('should create an instance', () => {
    const pipe = new CadmusProperNamePipe();
    expect(pipe).toBeTruthy();
  });
});
