import { PhysicalSizePipe } from './physical-size.pipe';

describe('PhysicalSizePipe', () => {
  it('should create an instance', () => {
    const pipe = new PhysicalSizePipe();
    expect(pipe).toBeTruthy();
  });
});
