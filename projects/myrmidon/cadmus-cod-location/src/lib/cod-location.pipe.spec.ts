import { CodLocationPipe } from './cod-location.pipe';

describe('CodLocationPipe', () => {
  it('should create an instance', () => {
    const pipe = new CodLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
