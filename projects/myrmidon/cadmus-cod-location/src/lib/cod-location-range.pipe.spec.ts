import { CodLocationRangePipe } from './cod-location-range.pipe';

describe('CodLocationRangePipe', () => {
  it('should create an instance', () => {
    const pipe = new CodLocationRangePipe();
    expect(pipe).toBeTruthy();
  });
});
